# Implementation Plans

Generated by the improve skill on 2026-08-15 from a four-track audit
(correctness, security, performance/architecture/tests,
dependencies/DX/direction) of commit `2dff7e6` (tag v1.0.0). Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Re-restore native session when planned prompt changes (+ projector budget arg) | P1 | M | — | DONE (2026-08-15, branch `advisor/001-restore-session-fingerprint`, worktree `.advisor-worktrees/plan-001`; detekt gate not applicable — no detekt Gradle wiring exists in this repo) |
| 002 | JNI: stop dropping `<...>`-shaped answer tokens | P1 | S | — | DONE (2026-08-15, branch `advisor/002-jni-control-token-filter`, worktree `.advisor-worktrees/plan-002`) |
| 003 | Vulkan service callback hardening + `.part` leak | P1 | S | — | DONE (2026-08-15, branch `advisor/003-vulkan-service-hardening`, worktree `.advisor-worktrees/plan-003`; deviation: File-based codec constructor for JVM testability — approved) |
| 004 | Inference/residency state integrity (counter leak, stale path, REPETITION reason) | P1 | S | — | DONE (2026-08-15, branch `advisor/004-inference-state-integrity`, worktree `.advisor-worktrees/plan-004`; deviation: InferenceUseCounter helper extraction — approved) |
| 005 | CI: run instrumented tests + wrapper validation | P1 | M | — | DONE (2026-08-15, branch `advisor/005-ci-verification-baseline`, worktree `.advisor-worktrees/plan-005`; deviation: ci.yml was untracked in main — branch tracks it for the first time, approved; NOTE: the existing `Run detekt` step references a task that doesn't exist in this repo and will fail CI — fix separately) |
| 006 | Characterization tests: PromptContextPlanner + mergeContinuation | P1 | S | — | DONE (2026-08-15, branch `advisor/006-planner-characterization-tests`, worktree `.advisor-worktrees/plan-006`; 14 tests pin current behavior, incl. overflow-check message and 12/320-char stitch boundaries) |
| 007 | VulkanRequestCodec round-trip tests | P2 | S | 003 | DONE (2026-08-15, branch `advisor/007-vulkan-codec-tests` stacked on 003, worktree `.advisor-worktrees/plan-007`; 8 round-trip cases added, 11 tests total, production untouched) |
| 008 | Backup crypto hardening (plaintext residue, caps, CharArray, strength hint) | P1 | M | 005 | DONE (2026-08-15, branch `advisor/008-backup-crypto-hardening`, worktree `.advisor-worktrees/plan-008`; format constants verified unchanged; deviations approved: FeatureViewModels.kt hosts MemoryViewModel, staging-root moved to noBackupFilesDir consistently; instrumented round-trip runs in CI after merge) |
| 009 | Manifest permission trim + label alignment | P2 | S | — | DONE (2026-08-15, branch `advisor/009-manifest-permission-trim`, worktree `.advisor-worktrees/plan-009`; fallbacks approved: health-connect-client dependency kept — MainActivity.kt also uses Health Connect APIs; QUERY_ALL_PACKAGES kept — collectors enumerate packages; label step no-op — committed manifest already said AIchat, audit premise was stale) |
| 010 | OTP redaction in notification capture | P2 | M | — | DONE (2026-08-15, branch `advisor/010-otp-notification-redaction`, worktree `.advisor-worktrees/plan-010`; conservative regex rules, wired at the single notification persistence point, 7 unit tests) |
| 011 | Batch attachment loads out of the messages Flow | P1 | M | — | DONE (2026-08-15, branch `advisor/011-streaming-attachment-batching`, worktree `.advisor-worktrees/plan-011`; deviations approved: deleteConversation also batched, test seam extracted for JVM testability; `attachmentsForMessage` now has no callers — deletion deferred) |
| 012 | Send-path perf (planner memoize, regex hoist, batched contexts) | P2 | M | 006, 011 | DONE (2026-08-15, branch `advisor/012-send-path-perf` stacked on 011+006, worktree `.advisor-worktrees/plan-012`; deviation approved: contextsForMessages takes a per-message promptFor lookup to preserve ranking; negative test proved the counting test catches double traversal) |
| 013 | Buffer thinking-delta state updates | P2 | M | 006, 001, 004 | DONE (2026-08-15, branch `advisor/013-thinking-delta-buffering` stacked on 012, worktree `.advisor-worktrees/plan-013`; deviations approved: BackendFallback buffer reset, idempotent flushThinking in both paths; one Regex("\\s+") at AiChatApp.kt:2275 left untouched — outside plan scope, trivial follow-up) |
| 014 | Memory-search candidate pre-filter + batched sources | P3 | M | — | DONE (2026-08-15, branch `advisor/014-memory-search-candidates`, worktree `.advisor-worktrees/plan-014`; deviation approved: memory pipeline extracted behind MemorySearchDataSource seam for JVM tests; ranking weights verified byte-identical; 7 freeze tests incl. stale-id drop) |
| 015 | Docs drift + repo hygiene | P2 | S | — | DONE (2026-08-15, branch `advisor/015-docs-repo-hygiene`, worktree `.advisor-worktrees/plan-015`; deviations approved: AGENTS.md was untracked — branch tracks the corrected version; instrumented-test count left at 6 — the 3 extra files exist only untracked in the main workspace; manifest labels left to plan 009) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 007 requires 003 because both edit `VulkanRequestCodec.kt`; the codec
  tests assume the `.part` cleanup exists.
- 008 requires 005 because its safety net (`OfficeBackupInstrumentedTest`
  round-trip) must actually execute in CI; local unit tests can't cover
  the migration/export paths.
- 012 requires 006 (its Step 1 must not change planner behavior — the
  characterization tests prove it) and 011 (same DAO/repository files).
- 013 requires 006 and should land after 001/004 — all four edit
  `ChatTurnRunner.kt`; serialize them to avoid conflicts.
- 015's manifest-label item is skipped if 009 landed first.

## Suggested execution waves

1. **Quick wins (parallel)**: 002, 003, 004, 009, 015.
2. **Safety nets**: 005, 006 (then 007).
3. **Core correctness + chat hot path**: 001, 011 (then 012, 013).
4. **Security depth**: 008, 010.
5. **Later**: 014.

## Findings considered and rejected (do not re-audit)

- **conversations.updatedAt index**: table holds tens of rows; index is noise.
- **Splitting `core/AppModels.kt`**: cohesive domain catalog, not a junk drawer.
- **kotlinx-immutable-collections for Compose stability**: strong-skipping mode makes the marginal gain unjustified vs fixing the state architecture.
- **`OfficeActivityWorkers` collector boilerplate consolidation**: bodies are genuinely source-specific; a base class hurts readability.
- **Version-catalog upgrades**: catalog is current within the project's toolchain timeline; no major-version lag worth a plan.
- **Re-adding speech/overlay/Daily Brief**: deliberately removed pre-1.0 per README — settled decision; only cleanup was planned (015).

## Deferred (real findings, not selected this round — candidates for the next session)

- BUG-06: main-thread DB open + migrations + JNI load at startup (ANR risk; M/MED).
- BUG-10: attachment worker stale write-back race (MED confidence).
- BUG-12: memory dedupe check-then-act race (MED confidence; needs DB v17 unique index).
- DEP-01: Gradle dependency verification / lockfile.
- DEP-02: SQLCipher vendored-AAR provenance (hash pinning or Maven coordinate).
- DX-03: release pipeline (assembleRelease in CI; tag-driven signed release).
- DX-05: formatter/.editorconfig.
- DEBT-01: split the 3,231-line `ui/AiChatApp.kt` (one screen per PR).
- DEBT-02: deduplicate `send`/`continueResponse` turn lifecycle (~150 drifted lines; needs 006's tests first).
- DEBT-03: move chat-turn orchestration out of `ui.viewmodel`.
- PERF-06: `deleteConversation` cascade without transaction.
- TEST-03: per-step migration validation (blocked by missing v10 schema JSON; document + cover 9→16 assertions).
- TEST-05: memory-ranking weight freeze (partially delivered by 014 Step 3).
- DIR-01: user-imported GGUF models (design/spike).
- DIR-02: per-skill export/share (design/spike).

## Audit provenance

- Commit audited: `2dff7e6` (2026-08-15). Static analysis only; no
  vendored `llama.cpp/`, build artifacts, or on-device dynamic testing.
- Verified non-findings at audit time: keystore git-ignored; AIDL service
  non-exported; backup path traversal guarded; GCM IVs fresh;
  `allowBackup=false` + full extraction-rule exclusions; no cleartext; no
  WebView/JS bridges; HF token only to hardcoded HF URLs and never logged.
