# Implementation Plans — Round 2 (UX & perceived quality)

Generated 2026-08-15 from a **user-experience audit** of commit `619d7de`
(tag v1.0.4). Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, run its drift check
first, honor its STOP conditions, and update your row when done.

## Why this round exists

Round 1 (plans 001–015, all shipped and since deleted in `52c2ae1`) ran four
audit tracks: correctness, security, performance/architecture/tests, and
dependencies/DX/direction. **None of them was a user-experience track.**
Every finding below survived that entire cleanup because nothing was looking
for it. Numbering continues at 016 because round-1 branch names
(`advisor/001-…` … `advisor/015-…`) are recorded in merged commit messages;
reusing 001–015 would make the history ambiguous.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 016 | Move container + database init off the main thread | P1 | M | — | DONE (branch `advisor/wave1-ux-foundations`. AppContainer is now all-lazy and warmed on IO; `AppDatabase.kt` untouched — approved deviation from Step 1: moving *where* construction happens preserves the sweep→open→finish contract without editing the encryption path. Completing it required 017's async gate, because `AiChatViewModelFactory` otherwise dragged the database open back onto the main thread. Migration gate satisfied: `DatabaseMigrationInstrumentedTest` 4/4 PASS. Cold start 602-650ms → ~500ms on the phone.) |
| 017 | Splash screen and first-frame gate | P1 | S | 016 | DONE (branch `advisor/wave1-ux-foundations`. core-splashscreen; `Theme.Aichat.Starting` in values + values-night; `installSplashScreen()` before `super.onCreate()`; held on `containerWarm`. MainActivity's factory is `by lazy` and content is gated on warm — this is what removes the last main-thread database block. Bounded-failure path complete: warm-up runs in its own job and the flag releases on completion or after 8s, so a *hung* warm-up cannot strand the splash (the first version only handled warm-up *throwing*). Timeout never fires in practice. `AppInstrumentedTest` passes.) |
| 018 | StrictMode main-thread I/O gate (debug) | P1 | S | — | DONE (branch `advisor/wave1-ux-foundations`; thread + VM policies under `BuildConfig.DEBUG`, `penaltyLog()` only, installed before the `:vulkan` process check so that process is covered too. **It paid for itself immediately**: the first run surfaced a 64ms main-thread disk read that plan 016 had missed — `MainActivity.getModelSetupViewModel` → `AiChatViewModelFactory.create` → `AppContainer.inferenceBenchmarkRunner` (lazy, not warmed) → `DeviceContextIdentity.read()`. Fixed by warming every lazy the ViewModel factory can reach. After the fix, all app-attributed violations are 0ms.) |
| 019 | Composer draft: saveable and conversation-scoped | P1 | S | — | DONE (branch `advisor/wave1-ux-foundations`; draft moved into `ChatUiState.input` backed by SavedStateHandle, cleared via `clearDraft()` on a real conversation switch only. Scope grew: `clearDraft` was never called on switch at all, so staged **attachments and selected skills bled across conversations too** — fixed by the same change. Verified on the phone: typed BLEEDCHECK in conv B, switched to conv A, composer empty; ROTATESURVIVE survived a landscape/portrait round-trip.) |
| 020 | Navigation back stack + back handling | P1 | S | — | DONE (branch `advisor/wave1-ux-foundations`; `navigateTo` now pairs `popUpTo(findStartDestination) { saveState = true }` with `restoreState`, so leaving settings no longer pushes a duplicate CHAT entry. BackHandler added, enabled only while `isSending`, reusing the existing `onStop`. Verified on the phone: Settings → X close → back now exits to the launcher instead of returning to Settings.) |
| 021 | Attachment limit honesty | P2 | S | — | DONE (branch `advisor/wave1-ux-foundations`; new `stageAttachments(uris)` + pure `planAttachmentIntake()` counts against already-staged and reports 'Added X of Y'; MainActivity no longer does `uris.take(20)`; shared `MAX_MESSAGE_ATTACHMENTS`; disabled attach button now explains itself. 7 JVM unit tests. Correction to the plan's premise: `stageAttachmentNow` already reported the limit — the silent drop was only at the picker.) |
| 022 | Baseline Profile + profileinstaller | P1 | M | 018 | DONE (branch `advisor/wave1-ux-foundations`. Deviation, approved: the plugin is pinned to **1.5.0-rc01**, not the 1.4.1 stable line — 1.4.1 refuses to apply against AGP 9.2.1 with "Module `:app` is not a supported android module"; it is build-time only and never ships. Second deviation: Gradle's `generateReleaseBaselineProfile` could not run because MIUI blocks its session install (`INSTALL_FAILED_USER_RESTRICTED`), so the generator was driven via `adb install` + `am instrument` and the profile committed at `app/src/main/baseline-prof.txt`. Verified rather than assumed: merged ART profile went from **0 → 4232 app rules**, R8 expanded to 13500, and the shipped APK's `assets/dexopt/baseline.prof` grew 9124 → 10095 bytes. Profile is 4.9MB raw / ~303KB compressed. Startup timing delta NOT measured — that needs plan 023's macrobenchmark.) |
| 023 | Macrobenchmark: startup and streaming jank | P2 | M | 022 | DONE (branch `advisor/wave1-ux-foundations`. New `:benchmark` module; StartupTimingMetric cold/warm/hot plus a with/without-profile pair, and FrameTimingMetric scroll. Baselines in `benchmark/README.md`. **Measured result worth acting on: the plan-022 baseline profile is worth only ~10ms (~1.8%) of cold start on this device** — the round-2 backlog had argued it was the highest-value startup item, and measurement says otherwise. Settings scroll is clean (P50 2.0ms, P99 4.4ms). Deviation, approved by the owner against this plan's own STOP condition: one production line (`testTagsAsResourceId = true` at the app root) was needed because UiAutomator cannot see Compose testTags — semantics only, no runtime behaviour change. Not done: automated streaming measurement (needs the 4.8GB model and real inference) — a manual Perfetto protocol is documented instead; and `scrollMessageList` needs a seeded conversation to mean anything. CI is not gated on benchmarks.) |
| 024 | Streaming scroll smoothness | P1 | M | 023 | **NULL RESULT** (implemented and measured on the phone; delta is within noise, which this plan's own STOP condition calls out as a real finding). Before: early 15.0% janky / P50 14ms / P99 19ms, late 16.7% / 15ms / 18ms. After: early 17.1% / 15ms / 18ms, late 18.8% / 15ms / 19ms. No regression either — `ChatUiInstrumentedTest` 3/3 incl. `streamingDoesNotOverrideManualScroll`. The measurement also reframes the problem: during streaming the app renders only ~240 frames per 60s (~4fps) because that is the CPU token rate, so the per-token scroll churn was never the bottleneck. Code is kept for the coroutine-churn removal and the jump-button animation, NOT for a performance claim — safe to revert. |
| 025 | Incremental markdown rendering while streaming | P1 | M | 023 | **REJECTED — premise disproven** (Step 1 measurement, which this plan required before optimising). The claim was that markdown re-parse cost grows with answer length. Measured within one generation on the phone: early window (short answer) 15.0% janky, P50 14ms, P99 19ms; late window (long answer, multi-section with a rendered table) 16.7% janky, P50 15ms, P99 18ms. The curve is flat, so the O(n^2) re-parse either is not happening as theorised or is dwarfed by the ~4fps token rate. Plan 025's STOP condition says explicitly: report and stop rather than optimising anyway. No production change made. |
| 026 | Allow composing during generation | P2 | S | 019 | DONE (branch `advisor/wave1-ux-foundations`. Step 1 confirmed in writing before touching anything: `ChatViewModel.sendMessage` already returns early while `generationJob` is active, and `continueResponse` has the same guard, so the `!sending` flag on the *text field* was never what prevented a double send — the send button is a Stop button while sending. Field is now enabled during generation; attach stays guarded (the plan's safer default, since staging kicks off background work); Enter-inserts-newline made explicit rather than incidental. **Verified on the phone**: with an answer actively streaming and the button showing Stop, typing into the composer landed and the keyboard stayed up.) |
| 027 | Error message quality and retry affordances | P2 | M | — | DONE (branch `advisor/wave1-ux-foundations`. `report(Throwable)` no longer falls back to `javaClass.simpleName`, so users can no longer be shown the literal string 'SQLiteConstraintException'. Important nuance found while doing it: many throws here carry text written deliberately for users — `error("Attachments exceed the 500 MB message limit.")`, `require { "The private attachment file is missing." }` — so this is a filter, not a blanket replacement; a presentability check keeps those and rejects anything reading like plumbing. Originals are still logged in full, and diagnostics are unchanged. Errors now carry an optional action + severity: model loading gets a real Retry (it fails transiently and succeeds on a second attempt), and important failures use SnackbarDuration.Indefinite with a dismiss action instead of vanishing in 4s. 10 JVM unit tests. Not done: string-resource extraction/localisation — noted as out of scope in the plan and still outstanding.) |
| 028 | Haptics and honest disabled states | P3 | S | — | DONE (branch `advisor/wave1-ux-foundations`. The send button used `IconButton` + `.background(primary)` with a hardcoded `onPrimary` tint, so a disabled button was pixel-identical to an enabled one — visible in nearly every screenshot taken this session. Now `FilledIconButton` with `IconButtonDefaults.filledIconButtonColors()`, letting the component own its disabled colours. **Verified on the phone: with an empty composer the button now renders dimmed.** Haptics went from 0 call sites to 3: send/stop (ContextClick), generation complete (Confirm, keyed on the isSending true->false transition so it cannot re-fire on recomposition), conversation delete (LongPress). Kept deliberately sparse — haptics on every chip toggle would be noise. NOT verified: the haptics themselves cannot be confirmed from screenshots or logs; they need a human holding the device.) |
| 029 | Theming, insets, and accessibility pass | P3 | M | — | PARTIAL. **Done**: theme mode (System/Light/Dark) and dynamic colour are now DataStore preferences with UI in Settings > Runtime > Appearance; **verified on the phone** — tapping Light flipped the whole UI while the system stayed dark. Two ad-hoc `isSystemInDarkTheme()` colour sites were fixed to follow the *applied* palette instead, because the new override would otherwise paint dark badges on a light background. **Two plan premises were wrong.** (1) The plan said to default dynamic colour ON; `AichatTheme` carries a documented decision that the neutral palette is deliberate ("dynamic wallpaper colour would make private-work surfaces visually inconsistent from one device to the next"), so it is opt-in and defaults OFF. (2) The a11y finding was misleading: all 21 null `contentDescription`s are icons inside already-labelled buttons, which is correct — labelling them would make TalkBack announce everything twice. No a11y change was needed. **NOT done**: Step 3 insets systematisation (restructuring Scaffold insets across every screen is not safe without testing each one) and Step 5 TalkBack pass (needs a human). Both remain open — and the phone disconnected before they could be verified. **Note the insets concern may be cosmetic rather than a defect**: there are only 4 inset call sites and every screenshot taken across this session shows content correctly clear of the status and navigation bars. **RESOLVED as a verified non-issue**: checked on the device under gesture nav, 3-button nav (`navigation_mode 0`) and with the IME open, on a display with a camera cutout. Content clears the status bar, the cutout and the navigation bar in every case; `imePadding()` lifts the composer correctly. The existing 4 call sites are sufficient and a Scaffold-wide restructure would have been churn. Only the TalkBack pass (Step 5) remains, and it needs a human.) |
| 030 | Docs drift + detekt wiring | P3 | S | — | DONE (branch `advisor/wave1-ux-foundations`. Docs: 8 README + 2 AGENTS occurrences of "AIchat" became "wochat"; the one survivor is `release-signing/AIchat-release.jks`, a real path. AGENTS' DB-version claim verified accurate (17). **This plan's own premise about detekt was wrong**: it is not "declared but never applied" — `:app` applies it at line 5 with a config at `config/detekt/detekt.yml`, and `./gradlew detekt` runs fine. It fails on **5 pre-existing findings**: CyclomaticComplexMethod `ChatTurnRunner.send` (58), LongMethod `MemoryCenter` (408/400), TooManyFunctions `MemoryDao` (20/20), SwallowedException `OfficeActivityWorkers:741`, MaxLineLength `ChatViewModel:531` (the last from `619d7de`, not from this branch). Per the plan, findings were NOT fixed. The orphaned `Upload detekt reports` CI step is removed rather than a detekt step added — a blocking gate would fail every merge today, and a `continue-on-error` one would repeat exactly the mistake that hid a red instrumented suite for months. Follow-up DONE in a later commit: all 5 findings addressed (2 real fixes, 1 extraction, 2 documented judgement calls) and detekt is now a blocking CI step.) |
| 031 | App crashes when started while the device is locked | P1 | M | 016 | DONE (branch `advisor/wave1-ux-foundations`; typed `DatabaseLockedException` + `Context.isDeviceCurrentlyLocked()` using KeyguardManager; startup defers behind the keyguard and retries on ACTION_USER_PRESENT; `ModelResidencyController`'s three `userManager.isUserUnlocked` guards corrected to also require the device be unlocked *now* — the pre-existing guards used the Direct Boot signal, exactly the trap the plan warned about; `ModelResidencyService` no longer touches the container's lazy chain while locked. **Verified on the phone: launch while locked → process ALIVE, 0 fatals** (baseline: process DEAD, InvalidKeyException). Step 4 recovery VERIFIED on the phone: the same deferred process (pid 18300) became fully functional after a manual unlock, with no relaunch and 0 fatals across the whole locked-to-unlocked cycle. Step 5 message is implemented but not visually confirmable — a normal activity is not shown over a secure keyguard, so it only covers the edge case where the activity resumes while isDeviceLocked() is still true. Step 6: no automated regression test — the instrumented suite is red (see 032); the manual adb procedure is documented in the plan's "Commands you will need".) |
| 032 | Instrumented suite is red on main and nothing notices | P1 | M | — | DONE (branch `advisor/wave1-ux-foundations`; all 7 ChatViewModel failures were one root cause — no test main dispatcher, so `viewModelScope` collectors had not run when assertions sampled state; fixed with `Dispatchers.setMain(UnconfinedTestDispatcher())` + an awaited state for the DataStore-backed case. Backup failure was a stale fixture describing an `attachments` table shape that never shipped — production code was correct and untouched. 48/57 verified green on the device (ChatViewModel 5 runs in a row); the remaining 9 launch Activities and are blocked by a MIUI background-activity-start restriction, not by code. `continue-on-error` removed from the CI job. **FINAL: the full suite runs 57/57 OK on the device** once MIUI's "Display pop-up windows while running in background" is granted — that permission was blocking the 3 activity-launching classes. One extra failure surfaced in the full run (`AppInstrumentedTest#settingsNavigationShowsModelManagement` scrolling to the Gemma row before `ensureOfficialRecords` had seeded it) and was fixed test-side after confirming the product renders the catalog correctly on a real first launch. A run in between died with "Process crashed" — that was MIUI killing the app via `SwipeUpClean`, not a defect.) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 017 requires 016: the splash's `keepOnScreenCondition` is only correct once
  container init is asynchronous; wiring it first would hide a main-thread
  stall behind a splash instead of fixing it.
- 022 should land after 018 so StrictMode is already catching main-thread I/O
  when the profile generator runs.
- 023 requires 022 (same `benchmark/` module and Gradle plugin wiring).
- 024 and 025 both require 023 — each claims a smoothness win, and without
  `FrameTimingMetric` numbers neither can be shown to have worked. They also
  both edit `ChatScreen`/`MessageBubble` in `ui/AiChatApp.kt`; serialize them.
- 026 requires 019, because letting the user type during generation is only
  safe once the draft is conversation-scoped and survives recomposition.

## Suggested execution waves

1. **Trust** (parallel): 016, 019, 020, 021 — then 017 after 016.
2. **Instrumentation**: 018, then 022, then 023. Land before wave 3 so the
   streaming work has a scoreboard.
3. **Streaming feel** (serialize): 024, then 025, then 026. Plus 027.
4. **Polish** (parallel): 028, 029, 030.

## Explicitly deferred to a later round

- **DEBT-01: split the 3,481-line `ui/AiChatApp.kt`.** Deliberately *not*
  first. Plans 019/020/024/025/026/028 all make surgical edits inside it;
  splitting first would force every one of them to rebase onto moved code.
  Split afterwards, when the split can be a pure file move.
- Round-1 deferred items that remain untouched and still valid: BUG-10
  (attachment worker write-back race), BUG-12 (memory dedupe race), DEP-01
  (dependency verification), DEP-02 (SQLCipher AAR provenance), DX-03
  (release pipeline), DEBT-02/03 (turn-lifecycle dedupe), PERF-06, TEST-03,
  DIR-01/02.

## Audit provenance

- Commit audited: `619d7de` (2026-08-15, v1.0.4). Static reading only — no
  device run, no profiler, no Perfetto trace. Severity ordering for 016, 024
  and 025 is reasoned from the code, not measured; plan 023 exists precisely
  to replace that reasoning with numbers.
- Every finding was re-verified against `619d7de` after an initial pass had
  read a stale worktree view. Line references in these plans are from
  `619d7de`.
- Not re-audited: correctness, security, and dependency tracks (round 1
  covered those). Vendored `llama.cpp/` untouched as always.

## Deliberate non-goals

- **String-resource extraction / localisation** (raised by plan 027). There are
  273 string literals in `ui/AiChatApp.kt` against 4 existing string resources.
  Extracting them is a large mechanical sweep with real typo risk across a
  3,481-line file, and this app is single-user and English-only by design. It
  buys nothing until a second language is actually wanted. Recorded as a
  decision, not an oversight.
| 033 | Remove phone sources entirely | P1 | L | — | DONE (commit follows this row). Deleted `activity/` (5 files) and `brief/HealthDataSource` — the latter existed only to feed the health collector. Gone with them: the notification-listener and accessibility services, `PackageChangeReceiver`, the a11y service config, the Health Connect dependency, and **12 of 19 manifest permissions** (location x3, activity recognition, contacts, calendar, health x3, usage stats, query-all-packages, telephony feature). **Verified on the phone**: `dumpsys package` now lists only network/notification/service/wake-lock permissions. Schema 17 -> 18 drops `activity_events`, `memory_summaries`, `collector_checkpoints` AND deletes memory_items whose *every* source was ACTIVITY — dropping the tables alone would have been a no-op for latency, since the collectors' output had already been compacted into ordinary memory rows. Rows with a mixed (chat + activity) provenance are kept; an instrumented migration test asserts both halves. **Measured, same "What is on my calendar today" question that cost 16 memories / 1578 tokens before: now 60 tokens for the turn.** A warm follow-up turn: "Session reuse accepted: 42 of 42 history messages already decoded", first token ~14.1 s (was ~39.6 s before `e4e1630`). One non-obvious keeper: `ActivityRetentionWorker` was mostly collector compaction but also ran `purgeExpiredMemories`, which still matters for chat memories — extracted to `memory/MemoryMaintenanceWorker` + `MemoryWorkScheduler`, which also cancels the 7 retired unique works so WorkManager stops firing at deleted classes. Tests/lint/detekt/assembleRelease green; 6 helpers left dead by the removal were deleted to keep detekt clean. **Still open (034)**: the remaining ~290-token preamble is chat-derived memories — the Memory screen shows every chat message stored as an Episode, including one-word replies. |
| 034 | Rebuild memory retrieval on embeddings | P1 | L | 033 | DONE and VERIFIED ON DEVICE. Lexical half: relevance separated from query-independent priors (importance+confidence+recency summed to ~0.104, above the old 0.08 floor, so every recent memory passed regardless of the question), top-k 4, 192-token cap, and `isWorthRemembering()`. Embedding half: EmbeddingGemma 300M Q8 in its own llama context (`embeddings=true`, MEAN pooling, 768 dims, loads in ~1.6 s), vectors L2-normalized natively and stored as a BLOB (schema 19), brute-force scanned — **no SQLite vector extension**. Hybrid ranking is `max(lexical, rebased cosine)`. **Four defects were found by running it, none by review**: (1) stop words diluted overlap so "What coffee do I like" retrieved nothing and the model said "I do not know what you like" — fixed by scoring on content words only; (2) "What do I like?" was stored as a Preference because `inferType` matches substrings, so the question test now runs before classification; (3) `retireUnsupportedArtifacts()` deleted the freshly downloaded embedder — record and 318 MB file — on the next app start, because its supported-id and allowed-filename sets were hardcoded to the chat model, which silently wasted a completed, verified download; (4) `SEMANTIC_BASELINE` was **guessed at 0.55, above where real paraphrases land**, so semantic recall matched nothing. Measured on device against "I prefer green tea after lunch": unrelated query 0.290, true paraphrase 0.582 — recalibrated to 0.40, and the table is now in the KDoc. Also: the embedder load moved off the chat-model-load path, since the download normally completes while the chat model is already resident and it otherwise sat unused until a restart. **End-to-end proof**: "What beverage do I enjoy after eating" -> "You enjoy green tea after eating", with zero shared content words; an unrelated Rust question in the same session was not polluted. Per owner request the download control now lives under Settings > Models beside the vision projector. 172 unit tests, lint, detekt, signed release green. |
| 035 | Finish a turn after the app is closed | P2 | M | — | DONE and VERIFIED ON DEVICE. Turns moved from `viewModelScope` to a process-scoped coroutine owned by `AppContainer`, and `ChatTurnRunner` moved there with them — it was per-Activity, so a relaunched UI had no idea a turn was in flight and would start a second one on the single inference engine. **The actual blocker was found only by running it**: `BackHandler(enabled = state.isSending) { onStop() }` cancelled generation instead of leaving, which was right when a turn could not survive the Activity and is exactly backwards now. Residency notification gained a "Writing a reply" state with a Stop action. Measured: pressed back at token 160, then from the launcher watched 320/384/448/512/576/608 and reopened to the finished answer. Deliberately unchanged: switching conversations still ends the turn, and nothing new is ever *started* in the background. Known limit seen in the same run — HyperOS reclaimed the process from 3.7 GB to 1.5 GB RSS shortly after backgrounding; the existing PROCESS_DEATH -> CONTINUABLE recovery covers the case where that kills it. |
| 036 | Offline voice call mode | P2 | XL | 034, 035 | **STEPS 1-7 DONE AND VERIFIED ON DEVICE; Step 8 outstanding.** A spoken exchange works end to end with no network: **"Hello, can you hear me?" -> "I can hear you. How can I help you?"**, spoken aloud. Silero VAD segments the utterance, Whisper transcribes it, the *same* `ChatTurnRunner` a typed message uses runs the turn (tagged `TurnOrigin.VOICE`), and Piper speaks each sentence as it is generated; call screen has the phase animation, transcript toggle, memory indicator and tap-to-interrupt. **Step 1 (baseline) came from 037 and invalidated this plan's latency table**: warm TTFT is bimodal, not a fixed ~5.2 s — the common case is far better than the ~6 s this plan was designed around. **Runtime**: sherpa-onnx 1.13.5 covers VAD+ASR+TTS, vendored as the unmodified official AAR beside SQLCipher (k2-fsa publishes nothing on Maven Central; the only alternatives are third-party repackagings). 49 MB in-tree, **APK 23,348,328 -> 35,269,446 bytes (+11.66 MB)**, arm64-v8a only. **Models** ride the existing download path as one card ('English speech pack', 278.9 MB) because no user should reason about a 'joiner'; three narrow changes made that work — optional `absoluteUrl`/`archiveRootDirectory` on `OfficialModelSpec`, `GgufValidator` restricted to `.gguf` (it would otherwise reject every ONNX file and both tarballs), and a new `ArchiveInstaller` with a staging dir and a traversal guard mirroring `BackupPathSafety`. All artifacts verified by HTTP HEAD and pinned by sha256; verified to survive a force-stop. `RECORD_AUDIO` is the only permission added back to the manifest 033 shrank, requested at point of use. **Accuracy was measured, not assumed**: a streaming Zipformer (int8, US English) returned `HALLO KA NU YERI` for "Hello, can you hear me?" *after* level, sample rate and dropped-window count were all verified clean — the signature of an acoustic model mismatched to the speaker. Replaced with **VAD-segmented Whisper base.en**, which returns the sentence exactly at **424 ms for 1.41 s of audio (RTF 0.30)**. Accepted trade: no word-by-word transcript while speaking, and the VAD owns turn-taking with end-of-turn silence 0.25 s -> 0.6 s. TTS measured **RTF 0.14** and confirmed audible by the owner. **Six defects found by running it, none by review**, five of which presented as 'no error, nothing happens': R8 obfuscating sherpa's JNI-resolved config classes (release-only, killed the process, AAR ships an empty proguard.txt — **all future sherpa work must be smoke-tested on release**); the VAD gating the recogniser so a quiet VAD killed the feature silently; `ENCODING_PCM_FLOAT` capture returning zeros; `AudioSource.VOICE_RECOGNITION` delivering a dead line where plain `MIC` gave ~100x signal; `modelType="zipformer2"` asserted on a model that is not one, silently returning empty text; and `VoiceCallCoordinator` caching `isInstalled()` at construction so a later download stayed invisible while the screen sat on 'Starting'. Also fixed: `AppDatabase` mapped `"VOICE"` back to `TYPED`, which would have recorded every spoken turn as typed. **Voice choice**: owner had no preference, so v1 ships `libritts_r-medium` with one default speaker rather than six invented IDs — all 904 are in the one file, so a picker later is config, not a re-download. **RE-MEASURED AFTER 037's MAPPING FIX — call mode is now usable.** transcript -> first audio: **first turn of a call 50.5 s -> 7.4 s, warm turn 37.1 s -> 1.7 s**; a warm turn is transcription 347 ms + prefill 762 ms + **first token 113 ms** + synthesis 215 ms, so with the VAD's 0.6 s end-of-turn silence it is **~2.6 s from the user stopping to hearing a reply** — inside this plan's original 6 s budget. Three further defects were found by *using* it, all now fixed: (a) **the call answered itself** — `AudioTrack.write` returns when samples are buffered, not played, so the mic reopened into the reply's tail and 'The capital of France is Paris.' came back as a user turn 'Harris.', then 'information.', then 'on to.'; fixed by waiting on *cumulative* `playbackHeadPosition` plus a 300 ms settle (the first attempt compared it against a single sentence's frames, which the head is always past, so it drained nothing); (b) **utterances were silently dropped** when a turn was still running — the owner saw their words appear and vanish; speaking while busy is now **barge-in**, cancelling the running turn, and it says so on screen if it still cannot start; (c) **the transcript was wiped on every listen**, so a misheard question could not be read back — both halves of the exchange now persist. Also observed: **MIUI `SwipeUpClean` kills the app** if it is swiped from recents mid-call (known behaviour per 032, not a defect, but it makes testing look broken). Remaining cost is prefill — 4734 ms on a conversation's first turn, 762 ms warm — which is 037's territory. **Step 8 as originally measured fired this plan's STOP condition.** Measured transcript-to-first-audio on a fresh conversation: **50.5 s and 37.1 s**, of which Whisper is 370/363 ms and Piper 222/208 ms — **the whole speech stack is ~0.6 s, under 1.5 %**. The rest is the LLM. STOP says 'materially worse than ~6 s ... attack TTFT first, not keep building', so Steps 1-7 stand and no further speech tuning is warranted. Two aggravating factors are properties of this design, not the model: **speech waits for a whole sentence rather than the first token** (turn 2 hit first token at 19.5 s but first sentence at 36.9 s — 17 s of generation before a word could be said; speaking on a clause boundary or a token budget would recover much of it), and **037's degraded per-decode regime dominates** — the same feature measured **989 ms** to first token on a healthy turn versus 19-27 s here, so call latency is currently unpredictable by an order of magnitude. **Next work is 037's open question** (fixed multi-second cost per `llama_decode`, needs Perfetto and thread-scheduling analysis), not audio engineering. Gate: unit + lint + detekt + assembleRelease green. |
| 037 | Cut time to first token for voice turns | P1 | M | 034 | **STEP 1 DONE AND MEASURED; STEPS 2-3 BLOCKED ON 036.** **The answer is page eviction, not prompt length.** The 4.9 GB model is a file-backed mmap and the kernel reclaims it while the app is idle, so the next turn re-reads the evicted weights from storage. Proven with `getrusage` fault counters around every decode: **identical 63-token prompts took 2858 ms with 26 major faults and 4981/5242/5064 ms with 16 875/21 461/21 281 major faults** — same work, ~2x the time, ~84 MB of storage reads. `RssFile` was **2.0 GB immediately before one such turn and 2.9 GB immediately after**, i.e. the turn faulted ~856 MB back in. The app is not doing this: exactly one deliberate 'Released ... pages' at model load appears all session. **This resolves the plan's 'missing half' of the 5.2 s** — it was never a separate phase, it is prefill and the first token running at half speed on non-resident weights. It also subsumes the bimodal first token seen mid-session (3.7-7.5 s on token 1 while token 32 of the same generation cost 155 ms); sampling measured ≤3 ms throughout, so it was never the sampler. **Correction to an earlier claim in this row's first draft**: I reported prefill at '~11 tok/s, so the plan's 21 tok/s is wrong'. That was measured entirely in the evicted regime and does not replicate. **Both numbers are real and they are two regimes** — ~22 tok/s resident, ~12 tok/s evicted. The plan's constant was fine. Attribution of a warm turn otherwise: prompt planning **36-359 ms** (query embedding 11-18 ms), DB reads 0-2 ms, persistence 1-3 ms, `restoreSession` with KV reuse **1-2 ms**, prefill 45 ms/token resident vs 80-170 evicted, first token 80-206 ms resident. **The embedder — this plan's named prime suspect — is ~1 % of TTFT.** Best turns end to end **1222 ms and 1253 ms**; the same shape of turn cost **5.5-6.1 s** evicted. Thermal is a real but minor term: CPU 41 -> 92 C across a sustained answer moved per-token time 81 -> ~105 ms (~25 %), nowhere near the 2-4x swings. **The obvious fix was tried and REFUTED — this is the most useful result here.** A turn has a free warming window (typing, or speaking in call mode), so `madvise(MADV_WILLNEED)` over the model's VMAs was implemented, wired to the composer on a 20 s throttle, and measured. Issuing it costs 7 ms. Replaying the same conversation with vs without it: 7 tokens **4852 ms vs 419 ms**, 9 tokens **5391 vs 536**, 745 tokens **55 785 vs 36 154**, first token **~5000 ms vs 107 ms** — **up to 11x worse**. Advising 4.9 GB of readahead on a device that cannot hold 4.9 GB starts a read/evict storm that starves the decode; faults during those decodes were ~0, so they were not waiting on storage, they were being crowded out. Code removed rather than left unwired. A retry must be bounded and rate-limited and measured against that control. **The control run also identifies the real target**: the bad regime is a **fixed ~4.5 s per `llama_decode` call plus ~70 ms/token** — a 7-token and a 12-token decode cost the same. That, not 'the first token', is what the 3.7-7.5 s first tokens were; a continuous generation pays it once because its decodes are back to back. In the healthy regime the fixed term is simply absent: a clean ~50-60 ms/token from 7 tokens to 745, first token 107 ms. Finding the trigger needs Perfetto and a look at whether HyperOS parks the inference threads between decodes — it is not a prompt problem, and 036 should be built against this. **Lever priced anyway** (A/B, same conversation and process): Memory ON = 63-68 prompt tokens / 5.34 s prefill; Memory OFF = 13 tokens / 1.43 s. A ~50-token memory preamble is the second-largest controllable term, and retrieval is looser than assumed ('Name one animal.' pulled one). **STEP 2 DONE 2026-08-18, with a partly negative result.** Unblocked once 036 added `TurnOrigin.VOICE`. A spoken turn now plans with `ContextBudget.COMPACT` — 2 memories not 4, a 96- not 192-token memory block, no conversation summary. **One of the plan's three suggestions was rejected on measurement grounds**: a terser system prompt for voice would change the *stable KV prefix*, so a conversation mixing typed and spoken turns would decline session reuse and re-prefill on every switch, trading ~4 s for ~30 s; both budgets share one system prompt and a unit test pins it. **Recall verified in a fresh conversation**, so the fact could only come from retrieval: 'Do I like to drink after lunch?' -> 'Yes, you prefer green tea after lunch.' with the composer showing `Memory · 1` — this plan's STOP condition holds. **But the cap is not the lever**: prompt tokens 63-68 -> 67 (barely moved) and prefill 5.34 s -> 2.73 s, where the halving is the mapping fix (`minor=1808 major=0`) rather than the budget — retrieval had found only one memory, so a cap of 2 never bound. The budget guards the worst case (4 memories at 192 tokens ≈ 17 s of prefill), it does not save the common one. **Retrieval selectivity was then investigated and RULED OUT as the problem**: a general-knowledge question spoken into a call ('can you tell me 10 African countries names?') carried just **19 prompt tokens / 1237 ms**, and the next turn 14 tokens / 692 ms — the floor correctly retrieves nothing; the earlier 'Name one animal.' case was a near-duplicate of a past message stored as an episode, not a loose floor. **The waste was the packaging**: header ~35 tokens + `- [preference; source: Chat message] ` ~9 tokens around a ~7-token fact — **45 tokens of wrapper for 7 of information**. A compact turn now uses a short header keeping only the instruction that changes answers ('not from training') and drops type/source, which exist for the Memory screen. Measured, same question, fresh conversation: **67 -> 39 prompt tokens** with the answer still 'Yes, you prefer green tea after lunch.' and `Memory · 1` — no recall cost, since the facts are untouched. **Do not quote that run's clock as a latency win**: first audio was 71.8 s with `major=640/847` faults, so it measured eviction, not tokens. **NEXT: major faults are back and the mapping fix does not cover them.** That fix cured *minor* faults; it cannot stop the kernel evicting pages, and eviction is now likelier because the model holds ~5 GB mapped without releasing while Whisper (208 MB), Piper and the embedder compete for the same memory — turns alternate between ~70 and ~210 ms/token. `use_mlock` is the obvious candidate, with the caveat that locking ~5 GB on a phone may fail outright or provoke the low-memory killer, so it must be measured rather than assumed. **Step 3 (cap reply length in voice) remains unattempted.** Also found: **the Memory chip invalidates the KV prefix** (it lives in the system prompt via `MEMORY_DISABLED_NOTICE`), so one tap forced a **33.4 s** full replay; cold-session replay is 15-33 s, which the first turn of a call will pay. **FIXED 2026-08-18 — the app was discarding the mapping it had just paid for.** `load_model()` ended with an unconditional `MADV_DONTNEED` over the whole model mapping, while llama.cpp had already mapped the entire file with **`MAP_POPULATE`**; the loader threw that away and re-faulted it during the user's first turn. Removing that one call, measured identically on a fresh process: first 22-token prefill **7511 ms / 52 769 minor faults -> 2013 ms / 2 088**, and **first generated token 4109 ms -> 164 ms** (97 ms on the next turn). **The first-token penalty — the largest single term in voice-call latency — is gone.** The two releases with a real reason are kept (before projector init, and `onTrimMemory`); RssFile settled at 3.27-3.36 GB with the process stable, so holding the mapping does not trip HyperOS reclaim. This also retires the whole 'warming' line of attack: the pages never needed hinting or prefetching, only not to be discarded. **MECHANISM IDENTIFIED 2026-08-18, from the owner's observation** ('if I close the app and open it, it's fast... sometimes it generates fast and sometimes it takes too long'): the bimodal regime is **page-table warm-up, not throttling**. On a fresh process with CPU sampled from `/proc/<pid>/stat`, the app held **5.06 cores saturated** in the `top-app` cpuset throughout — never starved — while the opening decodes cost **7511 ms (52 769 minor faults)** and **5099 ms (9 126)**, and generated tokens 32-128 then cost **164-172 ms with ~0 faults**. Minor faults are pages already in the page cache being mapped into the process for the first time: the opening decodes map ~250 MB, after which the model runs fast. This corrects the emphasis above — major faults (storage reads) are real, but the dominant reproducible start-of-session cost is *mapping*, not reading, which is also why `MADV_WILLNEED` made things worse: it hints readahead into the cache, and the pages were already cached, just not mapped. Next to try, in order: `MAP_POPULATE` at mmap time or a rate-limited sequential touch on a background thread once the model is resident, then `use_mlock`. Measure against the plan's table, not intuition — the last obviously-right idea was 11x slower. Shipped with the measurement kit: `TurnTrace` (JVM phase marks, `AIchatTurn` tag, durations only — never message text), embed timing, and sample/decode/major-fault counters in `aichat_jni.cpp`. Gate: unit + lint + detekt + `assembleRelease` green, `ChatViewModelInstrumentedTest` 14/14 on device; the full 57 died with `Process crashed` on `AppInstrumentedTest`, the MIUI background-activity-start restriction from 032, which needs a human to re-grant the popup permission after Gradle reinstalls the debug package. Memory recall re-verified end to end ('What do I like to drink after lunch?' -> 'You like green tea after lunch.', across conversations). |
